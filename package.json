{
  "name": "react-hello-word",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-redux": "^7.1.1",
    "react-router": "^5.0.1",
    "react-router-dom": "^5.0.1",
    "react-scripts": "5.0.1",
    "redux": "^4.0.4"
  },
  "devDependencies": {
    "env-cmd": "^10.0.1",
    "json-server": "^0.17.3",
    "faker": "^4.1.0"
  },
  "scripts": {
    "start": "npx env-cmd -f config/dev.env react-scripts start",
    "build:test": "npx env-cmd -f config/test.env react-scripts build",
    "build:prod": "npx env-cmd -f config/prod.env react-scripts build",
    "test": "react-scripts test",
    "mock-server": "npx json-server test/mockData.js --port 1234",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
